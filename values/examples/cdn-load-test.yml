config:
  target: "https://du2xtenyij8hn.cloudfront.net"
  phases:
    - duration: 300      # Increase duration to 10 minutes
      arrivalRate: 5000  # Increase arrival rate to 5000 requests per second
scenarios:
  - flow:
      - get:
          url: "/"
      - get:
          url: "/api/endpoint1"
      - get:
          url: "/api/endpoint2"
      - get:
          url: "/images/random-image.jpg"
      - post:
          url: "/api/submit"
          json:
            field1: "value1"
            field2: "value2"